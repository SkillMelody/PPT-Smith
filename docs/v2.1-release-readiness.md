# PPTSmith 2.1 Release Readiness

## Candidate

- Version: `2.1.2`
- Public release branch: `fix/pptsmith-2.1.2-static-scan`
- Standard readiness: accepted on macOS with LibreOffice 26.2.4.2.
- Premium readiness: run-specific; requires real render/readback, zero-error QA,
  rubric pass, and fallback disclosure.

## Verification evidence

- Public unit and schema suite: 223 passed.
- Public integration suite: 26 passed.
- Python compileall: passed.
- McKinsey known-defect regression:
  - old candidate rejected with 24 errors and 49 warnings;
  - RC1 rebuild rendered 10/10 with zero QA issues.
- Latin America report:
  - rendered 12/12;
  - zero warning/error;
  - editable core ratio 1.0.
- Future of Jobs Hero Showcase:
  - rendered 12/12;
  - structural inspection passed with zero warning/error;
  - 460 native shapes, 53 connectors, zero media files;
  - zero whole-slide rasterization;
  - internal visual rubric 17/18.

## ClawHub dry run

- Authenticated publisher: `SkillMelody`.
- Existing registry skill: `article-html-to-ppt`.
- Existing latest version: `2.0.7`.
- Candidate dry run: `would-publish`.
- Candidate version: `2.1.2`.
- Candidate file count: record from the final dry run.
- Candidate fingerprint: record externally in the release handoff. Do not
  embed it in the published skill tree because editing this file would change
  the fingerprint it attempts to report.
- Dry run used the exact isolated-worktree skill path, not the live workspace
  copy.

## GitHub readiness

The isolated development worktree belongs to a local workspace repository
without a Git remote. The verified public source repository is the separate
checkout `SkillMelody/MeowClawLab`, where this skill is published under
`Skills/article-html-to-ppt`.

- Sync only the accepted skill tree into the clean public repository checkout.
- Preserve public gallery assets and repository documentation that are not part
  of the isolated development tree.
- Commit and push the public repository first.
- Use that exact public commit as ClawHub source provenance.

## Required publication order

1. Sync the accepted tree to `SkillMelody/MeowClawLab`.
2. Push the exact accepted public commit.
3. Create GitHub tag/release `v2.1.2`.
4. Run ClawHub publish from the same accepted tree with:
   - `--version 2.1.2`
   - `--source-repo`
   - `--source-commit`
   - `--source-ref`
   - `--source-path Skills/article-html-to-ppt`
5. Verify the published registry version and moderation result.

The user authorized continuation of the release closeout after reviewing the
RC1 scope and evidence. Public publication must still use the exact verified
public commit and must stop on any failed verification or provenance mismatch.
